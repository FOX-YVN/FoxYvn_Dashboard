# AGENTS.md - FOX YVN Workspace

> Инструкции для AI-ассистентов (Codex CLI, Claude Code, и других) при работе с проектом FOX YVN Workspace

---

## 🎯 О ПРОЕКТЕ

**FOX YVN Workspace** — современная панель управления бизнесом с дизайном в стиле **Apple Music Desktop**.

### Основная информация
- **Тип**: Next.js 14 веб-приложение (App Router)
- **Назначение**: Dashboard для управления заказами, общения с клиентами, файлового хранилища, базы знаний, финансов и AI-ассистента
- **Дизайн**: Apple Music-inspired с glassmorphism эффектами
- **Акцентный цвет**: Красный `#FC3C44` (фирменный цвет Apple Music)
- **Статус**: В активной разработке

---

## 🌐 ЯЗЫК И СТИЛЬ ОБЩЕНИЯ

### Общение с пользователем
- **ВСЕГДА** общайся на **русском языке**
- Используй дружелюбный, но профессиональный тон
- **Объясняй что делаешь ПЕРЕД выполнением** действия
- Будь конкретным и информативным
- Не используй эмодзи в коде, только в сообщениях пользователю

### Комментарии в коде
- **ВСЕ** комментарии в коде ТОЛЬКО на **русском языке**
- Комментируй сложную логику подробно
- Используй JSDoc с описаниями на русском
- Формат:
```typescript
// Получаем список активных заказов из базы данных
const orders = await fetchOrders();

/**
 * Рендерит карточку заказа с информацией о клиенте
 * @param order - Объект заказа из БД
 * @returns JSX элемент карточки заказа
 */
function OrderCard({ order }: OrderCardProps) {
  // код компонента
}
```

---

## 🎨 ДИЗАЙН-СИСТЕМА: APPLE MUSIC DESKTOP

### Основные принципы дизайна

#### 1. Цветовая палитра
```css
/* Красный акцент (фирменный цвет Apple Music) */
--apple-music-red: #FC3C44;
--apple-music-red-hover: #F94C57;
--apple-music-red-pressed: #FA243C;

/* Нейтральные цвета (тёмная тема) */
--bg-primary: #000000;           /* Основной фон */
--bg-secondary: #0A0A0A;         /* Вторичный фон */
--bg-tertiary: #141414;          /* Карточки, панели */
--bg-elevated: #1C1C1E;          /* Поднятые элементы */

/* Текст */
--text-primary: #FFFFFF;         /* Основной текст */
--text-secondary: rgba(255, 255, 255, 0.6);  /* Второстепенный */
--text-tertiary: rgba(255, 255, 255, 0.4);   /* Подписи */
```

#### 2. Glassmorphism эффекты
```css
/* Лёгкое размытие - плавающие элементы */
.glass-light {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.1);
}

/* Среднее размытие - карточки и панели */
.glass-medium {
  background: rgba(20, 20, 20, 0.7);
  backdrop-filter: blur(40px) saturate(160%);
  border: 1px solid rgba(255, 255, 255, 0.08);
}

/* Сильное размытие - сайдбар и навигация */
.glass-strong {
  background: rgba(10, 10, 10, 0.85);
  backdrop-filter: blur(60px) saturate(140%);
  border-right: 1px solid rgba(255, 255, 255, 0.06);
}
```

#### 3. Типографика
- **Шрифты**: SF Pro Display (или системный `-apple-system, BlinkMacSystemFont`)
- **Размеры**: 11px (мелкие метки), 13px (вторичный), 15px (основной), 17-48px (заголовки)
- **Вес**: 400 (regular), 500 (medium), 600 (semibold), 700 (bold)

#### 4. Отступы и размеры
- **Spacing**: 8px базовая единица (4px, 8px, 12px, 16px, 24px, 32px, 48px, 64px)
- **Border Radius**: 6px (мелкие), 8px (кнопки), 12px (карточки), 16px+ (большие элементы)
- **Тени**: Используй `--shadow-sm`, `--shadow-md`, `--shadow-lg`, `--shadow-red`

#### 5. Анимации
- **Timing**: `cubic-bezier(0.4, 0, 0.2, 1)` — стандартное
- **Длительность**: 150ms (hover), 250ms (обычные), 400ms (появление)
- Всегда используй плавные переходы для hover эффектов

---

## 📦 ТЕХНОЛОГИЧЕСКИЙ СТЕК

### Frontend
- **Framework**: Next.js 14 (App Router)
- **Язык**: TypeScript (strict mode)
- **Стили**: Tailwind CSS + CSS Variables
- **UI компоненты**: Radix UI, shadcn/ui
- **Анимации**: Framer Motion, CSS transitions
- **Иконки**: Lucide React

### Backend
- **База данных**: SQLite (локально) / PostgreSQL (production)
- **ORM**: Prisma
- **Аутентификация**: NextAuth.js
- **Валидация**: Zod

### Инструменты
- **Менеджер пакетов**: Yarn
- **Линтер**: ESLint
- **Форматтер**: Prettier (если настроен)

---

## 📁 СТРУКТУРА ПРОЕКТА

```
📁 foxyvn-workspace/
├── 📁 app/
│   ├── 📁 (app)/              # Основные страницы (dashboard, comms, settings)
│   ├── 📁 (auth)/             # Авторизация (login, register)
│   ├── 📁 api/                # API Routes
│   └── 📄 globals.css         # Глобальные стили + CSS Variables
├── 📁 components/             # React компоненты (кнопки, карточки, формы)
├── 📁 lib/                    # Утилиты, типы, конфиги
├── 📁 hooks/                  # Custom React hooks
├── 📁 prisma/                 # Схема БД + миграции
├── 📁 modules/                # Подключаемые модули (ops, vault, mind, finance, ai-core)
├── 📁 public/                 # Статические файлы (images, fonts, manifest)
├── 📁 scripts/                # Вспомогательные скрипты
├── 📁 .cascade/               # История работы AI (логи, session notes)
├── 📄 .windsurfrules          # Правила для WindSurf Cascade
├── 📄 AGENTS.md               # Этот файл (инструкции для AI)
├── 📄 tailwind.config.ts      # Конфигурация Tailwind
└── 📄 next.config.js          # Конфигурация Next.js
```

### Важные файлы
- **`app/globals.css`**: CSS Variables дизайн-системы
- **`tailwind.config.ts`**: Кастомные цвета, шрифты, плагины
- **`prisma/schema.prisma`**: Схема базы данных
- **`.env`**: Переменные окружения (НЕ коммитить!)

---

## 🤖 РЕЖИМ РАБОТЫ AI АГЕНТОВ

### Автономность

**AI работает САМОСТОЯТЕЛЬНО без запроса разрешений для:**

✅ **ДЕЛАЙ СРАЗУ**:
- Изменение стилей и дизайна компонентов
- Редактирование существующих файлов
- Создание новых компонентов и утилит
- Рефакторинг кода для улучшения качества
- Исправление багов и проблем
- Добавление комментариев и документации
- Оптимизация производительности
- Обновление UI согласно дизайн-системе Apple Music

❌ **НЕ ДЕЛАЙ БЕЗ ПОДТВЕРЖДЕНИЯ**:
- Удаление файлов (кроме временных)
- Удаление функционала
- Изменение API endpoints
- Изменение схемы БД (`prisma/schema.prisma`)
- Изменение `.env` файлов
- Git операции (commit, push, merge)
- Установка новых зависимостей (кроме очевидно необходимых)

### Процесс работы

1. **Получил задачу** → Объясни план действий кратко (на русском)
2. **Анализируй** → Изучи существующий код и паттерны проекта
3. **Выполняй изменения** → Делай всё необходимое сразу
4. **Логируй** → Записывай действия в `.cascade/cascade-history.jsonl`
5. **Показывай результат** → Сообщи что сделано с примерами кода

---

## 📝 ЛОГИРОВАНИЕ И ИСТОРИЯ

### Обязательное логирование

**ПОСЛЕ КАЖДОГО значимого действия** добавляй строку в `.cascade/cascade-history.jsonl`:

```json
{
  "ts": "2026-01-19T12:34:56",
  "action": "edit",
  "file": "app/(app)/dashboard/page.tsx",
  "desc": "Изменил цвет фона карточки на glass-medium",
  "reason": "Приведение к стилю Apple Music",
  "result": "ok"
}
```

### Типы действий (action)
- `edit` — редактирование файла
- `create` — создание нового файла
- `delete` — удаление файла
- `refactor` — рефакторинг кода
- `style` — изменение стилей/дизайна
- `fix` — исправление бага
- `feature` — новая функциональность
- `config` — изменение конфигурации
- `install` — установка зависимостей

### Session Log

Если начинаешь новую сессию работы, создай/обнови `.cascade/session-log.md`:

```markdown
## Сессия: 2026-01-19 12:30

### Цель сессии
Привести дизайн dashboard к стилю Apple Music

### Выполненные изменения
- [12:35] Обновил цветовую схему кнопок
- [12:42] Добавил glass эффект карточкам заказов

### Текущие задачи
- [ ] Добавить анимацию hover для карточек
- [ ] Обновить стили сайдбара

### Проблемы
Нет проблем
```

---

## 🎯 ПРИОРИТЕТЫ РАЗРАБОТКИ

### 1. Дизайн превыше всего
- **Каждый элемент** должен соответствовать Apple Music стилю
- Используй **glassmorphism** и **backdrop-filter**
- Плавные **анимации** и **переходы**
- Внимание к деталям: отступы, размеры, тени, скругления

### 2. Производительность
- Оптимизируй изображения (WebP, lazy loading)
- Минимизируй re-renders (useMemo, useCallback)
- Используй Server Components где возможно (Next.js 14 App Router)
- Кэшируй данные (SWR, React Query, или Next.js cache)

### 3. Доступность (Accessibility)
- Семантичная HTML разметка
- ARIA атрибуты где нужно
- Keyboard navigation
- Достаточная контрастность текста

### 4. Responsive Design
- **Mobile-first** подход
- Breakpoints: `640px`, `768px`, `1024px`, `1280px`, `1536px`
- Адаптивная типографика
- Touch-friendly элементы на мобильных (минимум 44x44px)

### 5. Типизация TypeScript
- Всегда используй строгую типизацию
- Избегай `any`, используй `unknown` если тип неизвестен
- Создавай интерфейсы для пропсов компонентов
- Используй Zod для валидации данных из форм и API

---

## 🔍 ТИПИЧНЫЕ ЗАДАЧИ И ПРИМЕРЫ

### Задача 1: Создать новый компонент в стиле Apple Music

```typescript
// components/glass-button.tsx

/**
 * Стеклянная кнопка в стиле Apple Music
 * @param children - Содержимое кнопки
 * @param variant - Вариант стиля: 'primary' | 'secondary'
 */
export function GlassButton({
  children,
  variant = 'secondary',
  className,
  ...props
}: GlassButtonProps) {
  return (
    <button
      className={cn(
        "px-6 py-3 rounded-full font-semibold transition-all duration-150 active:scale-95",
        variant === 'primary' && "bg-apple-music-red hover:bg-apple-music-red-hover text-white shadow-red",
        variant === 'secondary' && "glass-light hover:bg-white/15 text-white border border-white/10",
        className
      )}
      {...props}
    >
      {children}
    </button>
  );
}
```

### Задача 2: Добавить glass эффект существующей карточке

```typescript
// Было:
<div className="bg-gray-800 p-4 rounded">
  {content}
</div>

// Стало:
<div className="glass-medium p-6 rounded-xl shadow-md hover:shadow-lg transition-all duration-250 hover:scale-[1.02]">
  {content}
</div>
```

### Задача 3: Создать страницу с правильной структурой

```typescript
// app/(app)/new-page/page.tsx

import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Название страницы | FOX YVN',
  description: 'Описание страницы',
};

export default function NewPage() {
  return (
    <div className="container mx-auto px-8 py-6">
      {/* Hero секция */}
      <header className="mb-8">
        <h1 className="text-3xl font-bold text-white mb-2">
          Заголовок страницы
        </h1>
        <p className="text-base text-secondary">
          Описание страницы
        </p>
      </header>

      {/* Основной контент */}
      <main className="grid gap-6">
        {/* Карточки, списки, формы и т.д. */}
      </main>
    </div>
  );
}
```

### Задача 4: Работа с Prisma

```typescript
// lib/db.ts - подключение к БД
import { PrismaClient } from '@prisma/client';

const globalForPrisma = global as unknown as { prisma: PrismaClient };

export const prisma =
  globalForPrisma.prisma ||
  new PrismaClient({
    log: ['query', 'error', 'warn'],
  });

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma;

// app/api/orders/route.ts - API endpoint
import { prisma } from '@/lib/db';

export async function GET() {
  try {
    // Получаем список заказов из БД
    const orders = await prisma.order.findMany({
      include: {
        customer: true,
        items: true,
      },
      orderBy: {
        createdAt: 'desc',
      },
    });

    return Response.json({ orders });
  } catch (error) {
    return Response.json({ error: 'Failed to fetch orders' }, { status: 500 });
  }
}
```

---

## ✅ ЧЕКЛИСТ ПЕРЕД ЗАВЕРШЕНИЕМ ЗАДАЧИ

Перед тем как сообщить о завершении задачи, проверь:

- [ ] Все изменения **залогированы** в `.cascade/cascade-history.jsonl`
- [ ] Обновлён `.cascade/session-log.md` (если это новая сессия)
- [ ] Код **прокомментирован** на русском языке
- [ ] Соблюдён **стиль Apple Music** (цвета, glassmorphism, анимации)
- [ ] **TypeScript типы** корректны (нет `any`, есть интерфейсы)
- [ ] Нет **console.log** в продакшн коде
- [ ] **Responsive** дизайн (проверь на разных размерах экрана)
- [ ] **Accessibility** учтено (semantic HTML, ARIA при необходимости)
- [ ] Код следует **паттернам** проекта (посмотри на существующие компоненты)

---

## 🚀 ЗАПУСК И РАЗРАБОТКА

### Локальный запуск
```bash
# Установка зависимостей
yarn install

# Настройка базы данных
yarn prisma generate
yarn prisma db push

# Заполнение тестовыми данными (опционально)
yarn prisma db seed

# Запуск dev сервера
yarn dev
```

Приложение доступно на **http://localhost:3000**

### Тестовый аккаунт
- **Email**: `john@doe.com`
- **Пароль**: `johndoe123`

### Полезные команды
```bash
yarn dev              # Запуск dev сервера
yarn build            # Production сборка
yarn start            # Запуск production сборки
yarn lint             # Проверка ESLint
yarn prisma studio    # Открыть UI редактор БД
yarn prisma db push   # Применить изменения схемы к БД
```

---

## 📚 ПОЛЕЗНЫЕ ССЫЛКИ

### Дизайн
- [Apple Design Guidelines](https://developer.apple.com/design/)
- [Apple Music цвета](https://developer.apple.com/design/human-interface-guidelines/color)

### Технологии
- [Next.js 14 Docs](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/)
- [Prisma ORM](https://www.prisma.io/docs)
- [Radix UI](https://www.radix-ui.com/)
- [Framer Motion](https://www.framer.com/motion/)
- [Lucide Icons](https://lucide.dev/)

---

## 🔒 БЕЗОПАСНОСТЬ

### Переменные окружения
- **НИКОГДА** не коммить `.env` файлы
- Используй `.env.example` для шаблона
- Храни секреты в 1Password или другом менеджере паролей

### База данных
- **Production**: PostgreSQL (удалённая БД)
- **Development**: SQLite (локальная, файл `prisma/dev.db`)
- Не используй production БД для тестов!

### Аутентификация
- NextAuth.js обрабатывает сессии
- Пароли хешируются (bcrypt)
- CSRF токены автоматически

---

## 📞 КОНТАКТЫ И ПОДДЕРЖКА

Если возникли проблемы или вопросы:
1. Проверь существующий код и документацию
2. Посмотри примеры в `.windsurfrules` и этом файле
3. Спроси у пользователя (e3fe3)

---

**Версия**: 1.0.0
**Дата создания**: 2026-01-19
**Автор**: e3fe3 (WSL Administrator)
**Проект**: FOX YVN Workspace - Apple Music styled business dashboard

---

*Этот файл является источником истины для всех AI-ассистентов, работающих с проектом FOX YVN Workspace. Следуй этим правилам для обеспечения консистентности и качества разработки.*
