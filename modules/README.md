# 📦 Модули FOX YVN

Эта папка содержит модули, которые можно подключить к Dashboard.

## Структура

```
modules/
├── ops/        # Операции (управление заказами)
├── vault/      # Хранилище файлов
├── mind/       # База знаний
├── finance/    # Финансы
└── ai-core/    # AI Ассистент
```

## Как подключить модуль

1. Скопируй папку модуля в `app/(app)/`
2. Добавь модуль в `lib/modules.ts`
3. Перезапусти dev-сервер

## Как создать новый модуль

Используй существующие модули как шаблон:

```bash
cp -r modules/ops modules/my-new-module
```

Затем отредактируй `page.tsx` под свои нужды.
